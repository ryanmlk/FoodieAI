
# Example env file (copy to .env locally; NEVER commit .env)

# If you ever use private HF models, place your token here:
# HUGGINGFACE_HUB_TOKEN=hf_********************************

# Optional: override embedding model (must match build & search)
# EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
